{
  "name": "garden",
  "version": "1.0.0",
  "description": "Messing about with WebGPU.",
  "scripts": {
    "wasm": "wasm-pack build -d ../garden-wasm simulation",
    "dev": "pnpm run --filter web dev",
    "dev:recomp": "pnpm run wasm && pnpm run --filter web dev",
    "build": "pnpm run wasm && pnpm run --filter web build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "vite": "^8.0.3"
  }
}